/**
 * Login action types.
 * @property {string} LOGIN - login action.
 * @property {string} REGISTER - register action.
 * @property {string} LOGOUT - logout action.
 */
export enum ELoginActions {
  LOGIN = 'LOGIN',
  REGISTER = 'REGISTER',
  LOGOUT = 'LOGOUT'
};
